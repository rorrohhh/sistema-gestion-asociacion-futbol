<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liga de Barrio - Gestión de Jugadores</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
    <style>
        body { background-color: #f0f2f5; }
        .main-card { border: none; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .table-header-custom { background: linear-gradient(45deg, #1a2a6c, #b21f1f); color: white; }
        .rut-font { font-family: 'Consolas', 'Monaco', monospace; font-weight: 600; color: #555; }
        .rol-badge { font-size: 0.9rem; background-color: #e3f2fd; color: #0d47a1; padding: 5px 10px; border-radius: 20px; font-weight: bold; }
        .club-tag { font-weight: 600; letter-spacing: 0.5px; }
        .table-hover tbody tr:hover { background-color: #fff8e1; transition: background-color 0.2s ease; }
        .numero-camiseta { font-weight: 900; color: #b21f1f; font-size: 1.1em; }
        
        /* Estilo para la nueva columna de edad */
        .edad-badge { 
            font-weight: bold; 
            color: #198754; 
            background-color: #e8f5e9;
            padding: 5px 10px;
            border-radius: 8px;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-trophy-fill text-warning me-2"></i> Fútbol de Barrio
            </a>
        </div>
    </nav>

    <div class="container pb-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h2 class="fw-bold text-dark">Plantilla de Jugadores</h2>
                <p class="text-muted mb-0">Listado oficial temporada 2025</p>
            </div>
            <a href="/inscribir" class="btn btn-success btn-lg shadow-sm">
                <i class="bi bi-person-plus-fill me-2"></i>Inscribir Jugador
            </a>
        </div>

        <div class="card main-card overflow-hidden">
            <div class="card-header bg-white p-4 border-bottom-0">
                <form action="/" method="GET" class="row g-3 align-items-center">
                    <div class="col-md-6">
                        <div class="input-group">
                            <span class="input-group-text bg-light border-end-0"><i class="bi bi-funnel-fill text-muted"></i></span>
                            <select name="club" class="form-select border-start-0 bg-light" onchange="this.form.submit()">
                                <option value="">Todos los Clubes</option>
                                <% if(clubes && clubes.length > 0) { %>
                                    <% clubes.forEach(function(c) { %>
                                        <option value="<%= c.id %>" <%= filtro == c.id ? 'selected' : '' %>><%= c.nombre %></option>
                                    <% }); %>
                                <% } %>
                            </select>
                            <button class="btn btn-primary px-4" type="submit">Filtrar</button>
                        </div>
                    </div>
                    <% if(filtro) { %>
                        <div class="col-auto">
                            <a href="/" class="btn btn-outline-danger btn-sm"><i class="bi bi-x-circle me-1"></i> Borrar filtro</a>
                        </div>
                    <% } %>
                    <div class="col text-end text-muted d-none d-md-block">
                        <small>Mostrando <strong><%= jugadores ? jugadores.length : 0 %></strong> jugadores</small>
                    </div>
                </form>
            </div>

            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-header-custom">
                        <tr>
                            <th class="py-3 ps-4">N°</th>
                            <th class="py-3">Jugador</th>
                            <th class="py-3">RUN</th>
                            <th class="py-3 text-center">Rol</th>
                            <th class="py-3 text-center">F. Nacimiento</th>
                            <th class="py-3 text-center">Edad</th>
                            <th class="py-3">Club</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white">
                        <% if (jugadores && jugadores.length > 0) { %>
                            <% jugadores.forEach(function(jugador) { %>
                                
                                <% 
                                    let edad = "--";
                                    if (jugador.fecha_nacimiento) {
                                        const hoy = new Date();
                                        const nacimiento = new Date(jugador.fecha_nacimiento);
                                        let anios = hoy.getFullYear() - nacimiento.getFullYear();
                                        const mes = hoy.getMonth() - nacimiento.getMonth();
                                        if (mes < 0 || (mes === 0 && hoy.getDate() < nacimiento.getDate())) {
                                            anios--;
                                        }
                                        edad = anios;
                                    }
                                %>

                                <tr>
                                    <td class="ps-4"><span class="numero-camiseta"><%= jugador.numero %></span></td>
                                    <td>
                                        <div class="fw-bold text-dark"><%= jugador.apellido_paterno %> <%= jugador.apellido_materno %></div>
                                        <div class="text-muted small text-uppercase"><%= jugador.nombres %></div>
                                    </td>
                                    <td>
                                        <% if(jugador.rut_num) { %>
                                            <span class="rut-font"><%= jugador.rut_num.toLocaleString('es-CL') %>-<%= jugador.rut_dv %></span>
                                        <% } else { %>
                                            <span class="text-muted">--</span>
                                        <% } %>
                                    </td>
                                    <td class="text-center"><span class="rol-badge border"><%= jugador.rol_asociacion %></span></td>
                                    
                                    <td class="text-center text-muted">
                                        <% if(jugador.fecha_nacimiento) { %>
                                            <i class="bi bi-calendar3 me-1"></i>
                                            <%= new Date(jugador.fecha_nacimiento).toLocaleDateString('es-CL') %>
                                        <% } else { %>
                                            --
                                        <% } %>
                                    </td>

                                    <td class="text-center">
                                        <% if(edad !== "--") { %>
                                            <span class="edad-badge"><%= edad %> años</span>
                                        <% } else { %>
                                            <span>--</span>
                                        <% } %>
                                    </td>

                                    <td><span class="badge rounded-pill text-bg-secondary club-tag px-3 py-2"><%= jugador.nombre_club %></span></td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr><td colspan="7" class="text-center py-5">No se encontraron jugadores.</td></tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
            <div class="card-footer bg-light text-center py-3 text-muted small">Sistema de Gestión Deportiva &copy; 2025</div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>